spring:
  application:
    name: ProjetoOracleXESpringBoot

  datasource:
    url: jdbc:oracle:thin:@localhost:1521/XEPDB1
    username: dev_user
    password: 123
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
        format_sql: true